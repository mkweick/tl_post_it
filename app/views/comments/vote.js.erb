$('#comment-<%= @comment.id %>-vote').html(
  '<%= link_to vote_post_comment_path(@post, @comment), method: "delete",
                                                  remote: true,
                                                  class: "btn btn-mini" do %>\
    <i class="icon-thumbs-up icon-blue"></i>\
    <%= @comment.reload.votes_count %>\
  <% end %>'
);