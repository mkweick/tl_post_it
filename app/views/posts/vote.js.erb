$('#post-<%= @post.slug %>-vote').html(
  '<%= link_to vote_post_path(@post), method: "delete", remote: true,
                                          class: "btn btn-mini" do %>\
    <i class="icon-thumbs-up icon-blue"></i>\
    <%= @post.reload.votes_count %>\
  <% end %>'
);